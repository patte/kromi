# Rendered by narchy so firefox.sh knows which side of light and dark a
# palette sits on. Not a Firefox config: the browser has no include for prefs,
# and which appearance it hands to websites is one numbered pref rather than a
# colour, so the value is copied into the profile's user.js rather than
# sourced from here.
mode={{ mode }}
