# Rendered by kromi so vlc.sh knows which side of light and dark a palette
# sits on. Not a VLC config: dark mode there is a single boolean rather than a
# set of colours, and vlcrc is a file VLC owns outright, so the value is copied
# into it rather than sourced from here.
mode={{ mode }}
